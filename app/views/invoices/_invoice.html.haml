- if (params['controller'] == 'invoices') and (template = template_for_current_view).present?
  = render(template, :invoice => invoice)
- else
  -# default view
  = render 'invoices/index_brief', :invoice => invoice
