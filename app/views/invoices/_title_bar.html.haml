#confirm{ hidden }
.title_tools#menu
  = link_to_inline(:edit_invoice, edit_invoice_path(@invoice), :text => t(:edit)) + " | "
  = link_to_function(t(:delete) + '?', confirm_delete(@invoice))
.title_tools#buttons
  = view_buttons
.title#edit_invoice_title
  = h(@invoice.full_name)
  = image_tag("loading.gif", :size => :thumb, :id => "loading", :style => "display: none;")

%div#edit_invoice{ hidden }
